spring.application.name=gcpCollector
server.port=8095

# Spring Batch Job enable
spring.batch.job.enabled=false
spring.batch.jdbc.initialize-schema=always

spring.datasource.driver-class-name=org.mariadb.jdbc.Driver
spring.datasource.url=jdbc:mariadb://localhost:3306/cost?autoReconnect=true&allowMultiQueries=true&useSSL=false&rewriteBatchedStatements=true
spring.datasource.username=mcmpcostopti
spring.datasource.password=0000

# MyBatis
mybatis.mapper-locations=classpath:/mapper/**/*_SQL.xml
# 환율(KRW->USD): 매퍼 XML의 ${krwPerUsd} 치환에 사용 (env: EXCHANGE_KRW_PER_USD, 기본 1400.0)
mybatis.configuration-properties.krwPerUsd=${EXCHANGE_KRW_PER_USD:1400.0}

# Scheduler
gcpBillingCronSchedule=0 0 8 * * ?

# Alarm Service
costopti.alarmservice.url=${ALARM_SERVICE:alarm_service}

# Tumblebug
tumblebug.url=${TUMBLEBUG_URL:http://mc-infra-manager:1323/tumblebug}
tumblebug.username=${TUMBLEBUG_USERNAME:default}
tumblebug.password=${TUMBLEBUG_PASSWORD:default}

# GCP BigQuery
gcp.project-id=${GCP_PROJECT_ID:}
gcp.client-email=${GCP_CLIENT_EMAIL:}
gcp.private-key-id=${GCP_PRIVATE_KEY_ID:}
gcp.private-key=${GCP_PRIVATE_KEY:}
gcp.dataset=${GCP_BQ_DATASET:}
gcp.table=${GCP_BQ_TABLE:}

# OpenBao 크레덴셜 연동
# true  : env 무시하고 OpenBao 에서 바로 조회
# false : env 우선 → 없으면 OpenBao 폴백 → 둘 다 없으면 ERROR 로그 (기본)
openbao.enabled=${OPENBAO_ENABLED:true}
openbao.address=${OPENBAO_ADDRESS:}
openbao.token=${OPENBAO_TOKEN:}
